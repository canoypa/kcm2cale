import { Box, Typography } from "@material-ui/core";
import { FC } from "react";

export const SadFace: FC = () => {
  return (
    <Box padding="1rem">
      <Typography variant="h2">:(</Typography>
    </Box>
  );
};
