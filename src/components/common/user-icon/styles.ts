import { css } from "@emotion/css";

export const container = css({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  width: 48,
  height: 48,
});

export const icon = css({
  width: 32,
  height: 32,
  borderRadius: 16,
  overflow: "hidden",
});
