import { css } from "@emotion/css";

export const container = css({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});

export const icon = css({
  display: "inline-flex",
  borderRadius: 16,
  overflow: "hidden",
});
