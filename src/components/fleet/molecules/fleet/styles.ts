import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  toggleFleetArea: {
    margin: "0px 8px 8px",
  },

  shipsList: {
    display: "flex",
    flexDirection: "column",
    // rowGap: 8,
  },
}));
