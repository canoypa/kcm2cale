import { css } from "@emotion/css";

export const root = css({
  position: "fixed",
  width: "100vw",
  height: "100vh",
  backgroundColor: "#fff",
  overflow: "auto",
});

export const list = css({
  maxWidth: 1279,
  margin: "0 auto",
});

export const searchAdjust = css({
  marginBottom: 104,
});
