import { css } from "@emotion/css";

export const container = css({
  display: "flex",
  justifyContent: "center",
});

export const content = css({
  display: "grid",
  rowGap: 16,
  padding: 24,
  width: "100%",
  maxWidth: 1200,
});
